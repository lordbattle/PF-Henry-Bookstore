const axios = require ("axios");
const { User, Book, Genre, Author, ReviewStore } = require("../db");
const { Op } = require("sequelize");

const saveAllBooksDb = () => {

}

module.exports = {
  saveAllBooksDb,
};
